#ifndef __sort_characters_by_frequency_h__
#define __sort_characters_by_frequency_h__

#include <string>
#include <vector>
using std::string;
using std::vector;

class sort_characters_by_frequency {
private:
public:
    sort_characters_by_frequency();
    ~sort_characters_by_frequency();
    string frequencySort(string s);
};

#endif