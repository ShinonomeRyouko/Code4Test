#ifndef __min_window_substr_h__
#define __min_window_substr_h__

#include <string>
using std::string;
class min_window_substr {
private:
public:
    min_window_substr();
    ~min_window_substr();

    string minWindow(string s, string t);
};

#endif