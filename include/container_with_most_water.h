#ifndef __container_with_most_water_h__
#define __container_with_most_water_h__

#include <vector>
using std::vector;

class container_with_most_water {
private:
public:
    container_with_most_water();
    ~container_with_most_water();
    int maxArea(vector<int>& height);
};

#endif