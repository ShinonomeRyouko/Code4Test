#ifndef __maximal_rectangle_h__
#define __maximal_rectangle_h__

#include <vector>

using namespace std;
class maximal_rectangle {
private:

public:
    maximal_rectangle();
    ~maximal_rectangle();
    int maximalRectangle(vector<vector<char>>& matrix);
};

#endif