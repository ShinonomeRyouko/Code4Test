#ifndef __sort_colors_h__
#define __sort_colors_h__

#include <vector>
using std::vector;

class sort_colors {
private:
public:
    sort_colors();
    ~sort_colors();
    void sortColors(vector<int>& nums);
};

#endif